name: zebra_rfid
description: Zebra RFC2000-TC20/RFD8500 RFID模块插件 .
version: 0.0.4
repository: https://github.com/jiang6641/zebraRfid
homepage:

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.2.0
  json_serializable: ^6.3.1

flutter:
  plugin:
    platforms:
      android:
        package: com.hone.zebra_rfid
        pluginClass: ZebraRfidPlugin

